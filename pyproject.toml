[project]
name = "qft-based-phase-estimation-on-quiskit"
version = "0.1.0"
description = "Mini project for QFT based estimation"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "plotly>=5.0",
    "nbformat>=4.2.0"
]
